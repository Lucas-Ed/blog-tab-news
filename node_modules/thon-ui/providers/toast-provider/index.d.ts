import React from 'react';
import toast, { Toast, ToastPosition } from 'react-hot-toast';
type Props = {
    title?: string;
    message: string;
    position?: ToastPosition;
    duration?: Toast['duration'];
    background?: string;
};
export type ToastContextType = {
    notify: (props: Props) => void;
    toast: typeof toast;
};
export declare const ToastContext: React.Context<ToastContextType>;
export declare const ToastProvider: React.FC<React.HTMLAttributes<HTMLElement>>;
export {};
